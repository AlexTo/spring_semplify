spring:
  application:
    name: entity-hub
  cloud:
    config:
      discovery:
        enabled: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/