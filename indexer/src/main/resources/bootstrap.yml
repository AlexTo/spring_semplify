spring:
  application:
    name: indexer
  cloud:
    config:
      discovery:
        enabled: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/